[project]
name = "article-enricher"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "celery>=5.5.3",
    "fastapi>=0.116.1",
    "httpx",
    "instructor>=1.10.0",
    "python-dotenv",
    "redis>=6.3.0",
    "uvicorn>=0.35.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
    "ruff>=0.12.7",
]
